name: ci
on:
  - push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: norio-nomura/action-swiftlint@3.2.1
  macOS-test:
    strategy:
      matrix:
        swift-version:
          - "6.0"
          - "6.1"
          - "6.2"
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - uses: swift-actions/setup-swift@v3
        with:
          swift-version: ${{ matrix.swift-version }}
      - run: swift test
  linux-test:
    strategy:
      matrix:
        swift-version:
          - "6.0"
          - "6.1"
          - "6.2"
    runs-on: ubuntu-latest
    container: swift:${{ matrix.swift-version }}
    steps:
      - uses: actions/checkout@v6
      - run: swift test
  iOS-build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - run: xcodebuild -scheme vincenty -destination 'platform=iOS Simulator,name=iPhone 17'
